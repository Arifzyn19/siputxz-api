function addNumbers(a: number, b: number): number {
    return a + b;
}

const num1: number = 5;
const num2: number = 10;

console.log(`The result of the addition ${num1} + ${num2} adalah:`, addNumbers(num1, num2));